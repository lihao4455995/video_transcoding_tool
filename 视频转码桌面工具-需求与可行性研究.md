# 本地视频转码桌面工具：需求报告与可行性研究

> 文档版本：v1.0  
> 调研日期：2026-09-04  
> 当前阶段：产品立项前研究，不含开发实现  
> 数据口径：GitHub 指标为调研日快照；厂商能力以公开官方文档为准，实际可用范围和价格仍需在采购/上线前复核。

## 1. 执行摘要

### 1.1 结论

开发一款“本地、隐私友好、兼顾新手与批处理用户”的视频文件转码桌面工具在技术上可行，建议立项，但第一版必须严格控制范围。

推荐方案不是自研编解码器，而是：

- 以 **FFmpeg/ffprobe** 作为媒体处理内核；
- 用桌面应用负责文件分析、参数约束、预设、任务队列、进度、错误恢复和结果校验；
- 默认以软件编码保证质量与兼容性，同时自动探测 NVIDIA NVENC、Intel Quick Sync、AMD AMF、Apple VideoToolbox 等硬件能力；
- 用“场景预设 + 专家参数”两层交互，避免把 FFmpeg 命令行原样暴露给普通用户；
- Windows 10/11 x64 首发，架构预留 macOS 与 Linux；所有媒体默认只在本机处理。

### 1.2 产品机会

现有工具通常位于两个极端：HandBrake 易用但工作流边界明确；Shutter Encoder/FFmpeg Batch 功能全面但信息密度高；LosslessCut 擅长无损操作但不是以完整转码为中心；云厂商能力强但需要上传、持续付费并依赖网络。可形成差异化的位置是：

1. 比专业前端更容易理解：直接提供“发到微信/网页播放/减小体积/剪辑软件中转/保留最高质量”等目标。
2. 比基础转换器更可靠：转码前做兼容性检查，转码后自动验证文件、时长、音轨及可播放性。
3. 比云服务更私密：素材不离开本机，无上传等待和云存储成本。
4. 比单纯 GUI 更透明：展示预计体积、硬件路径、质量/速度取舍及可复制的实际 FFmpeg 命令。

### 1.3 建议的 MVP

MVP 聚焦“可靠转码”，不做完整剪辑器、AI 修复、在线下载或云端协作：

- 拖放文件和文件夹、批量任务队列；
- ffprobe 媒体信息分析；
- MP4/MKV/WebM 输出；
- H.264、H.265、AV1（视本机能力）、AAC、Opus；
- 质量模式（CRF/CQ）和目标体积/目标码率模式；
- 分辨率、帧率、横竖屏、裁剪/补边、音轨/字幕选择；
- 转封装与流复制；
- 软件/硬件编码自动探测、明确显示、失败后可选择回退；
- 预设管理、队列暂停/取消/重试、日志、完成通知；
- 临时文件 + 原子落盘，默认不覆盖源文件；
- 输出自动校验和可解释错误信息。

### 1.4 可行性评级

| 维度 | 评级 | 判断 |
|---|---:|---|
| 技术可行性 | 高 | FFmpeg 生态成熟，开源产品已反复验证“桌面外壳 + 媒体内核”路线 |
| 产品可行性 | 中高 | 需求广泛，但同类工具多，必须靠易用、透明与可靠形成差异 |
| 交付可行性 | 中高 | 2～3 人可在 12～16 周完成 Windows MVP；跨平台和商用分发会增加工作量 |
| 法务可行性 | 中 | FFmpeg 构建选项、GPL/LGPL、编解码专利和商店政策必须在开发前确定 |
| 运营可行性 | 高 | 本地应用无需转码服务器；主要成本是签名、发布、崩溃收集与持续兼容测试 |
| 综合建议 | **有条件 Go** | 先完成许可证方案与硬件兼容性 PoC，再进入完整 MVP |

---

## 2. 研究范围与假设

### 2.1 当前假设

- 首发系统：Windows 10/11 x64。
- 后续系统：macOS Apple Silicon/Intel，Linux 作为社区或后续版本。
- 用户：内容创作者、运营人员、摄影用户、课程制作人员及有批处理需求的进阶用户。
- 核心价值：本地处理、简单可靠、批量高效、结果可验证。
- 商业模式尚未确定，因此技术和许可证分析同时覆盖开源免费与闭源商用两种可能。
- 不修改源视频，除非用户明确选择覆盖；默认生成新文件。

### 2.2 不在本报告范围

- 自研视频编码标准或编码器内核；
- 直播实时转码集群；
- 云端 CDN、DRM 密钥服务和多租户 IAM；
- 完整非线性剪辑、特效合成、多人协作；
- 各国家/地区编解码专利的正式法律意见。

---

## 3. 视频转码核心原理

### 3.1 文件、封装与码流不是一回事

用户看到的 `.mp4`、`.mkv`、`.mov` 是**容器/封装格式**。容器内部可以包含多个彼此独立的码流：视频、音频、字幕、章节、附件和元数据。H.264/H.265/AV1 是视频编码格式，AAC/Opus/MP3 是音频编码格式。

典型转码流水线：

```text
输入文件/流
  -> 解封装 Demux
  -> 选择视频、音频、字幕等码流
  -> 解码 Decode
  -> 像素/音频滤镜 Filter
  -> 编码 Encode
  -> 封装 Mux
  -> 输出文件/分片
```

其中：

- **转码（transcode）**：解码后重新编码，能改变编码、画质、分辨率、帧率，但耗时且通常有损。
- **转封装（remux）**：不解码，只把已有码流放入另一个容器，速度快且不损失质量，但受容器兼容性约束。
- **流复制（stream copy）**：FFmpeg 的 `-c copy` 路径，常用于换封装、移除音轨、抽取字幕和关键帧附近剪切。
- **滤镜处理**：缩放、裁剪、补边、旋转、去隔行、字幕烧录、响度归一化等发生在解码与编码之间。

### 3.2 质量、体积和速度的三角关系

同一种编码器下，画质、文件体积和编码速度无法同时最优。产品必须把这个取舍表达清楚：

- **CRF/CQ（恒定感知质量）**：以质量为目标，复杂画面使用更多码率，文件大小不固定；适合本地转码的默认模式。
- **VBR/ABR（可变/平均码率）**：围绕目标平均码率分配比特；适合带宽或体积有约束的场景。
- **CBR（恒定码率）**：强调码率稳定，适合特定传输链路，不是普通文件压缩的首选。
- **两遍编码**：第一遍分析复杂度，第二遍分配码率；更适合严格目标体积，耗时接近翻倍。
- **Preset（编码速度档位）**：更慢通常意味着同画质更小或同码率更好，而不是直接代表画质值。

产品层应避免承诺“无损压缩还能大幅变小”。真正无损编码通常体积较大；日常所谓“画质无损”多指肉眼近似无损。

### 3.3 软件编码与硬件编码

- 软件编码（如 x264、x265、SVT-AV1）主要使用 CPU，质量/码率效率通常较好、参数丰富，但速度较慢。
- 硬件编码使用 GPU/媒体引擎（NVENC、Quick Sync、AMF、VideoToolbox），速度快、CPU 占用低，适合批量和快速交付；相同码率下质量可能低于优秀的软件编码预设。
- 硬件能力取决于 GPU 代际、驱动、操作系统、FFmpeg 构建和像素格式，不能只按厂商品牌判断。例如 AV1 编码只在较新的硬件代际可用。
- “发现编码器名称”不等于真正可用。应用需要运行一个极短的实际编码测试，建立能力矩阵。

### 3.4 影响兼容性的关键点

- 容器与编码组合：并非任意编码都能放入任意容器。
- Profile、Level、位深与色度抽样：例如 H.264 8-bit 4:2:0 的终端兼容性通常最好。
- HDR：必须正确保留或转换色域、传递函数、母版显示和内容亮度元数据；错误处理会灰白、过曝或偏色。
- 可变帧率（VFR）：手机录屏和移动设备视频常见；强制恒定帧率可能造成音画同步或重复/丢帧问题。
- 旋转元数据：手机竖屏素材可能依赖显示矩阵而不是实际旋转像素。
- 关键帧/GOP：影响无损切点、拖动体验、分片和 ABR 切换。
- 时间戳：DTS/PTS、负时间戳、断裂时间轴是合并与剪切常见故障源。
- 多音轨与字幕：默认只取第一条流容易造成语言、评论音轨或字幕丢失。
- MP4 Fast Start：把 `moov` 元数据移至文件前部，可改善网页渐进播放起播速度。

### 3.5 桌面工具真正需要自研的部分

编码本身交给 FFmpeg。产品竞争力来自编码外围：

1. 用 ffprobe JSON 建立统一的媒体模型。
2. 根据输入、目标场景和本机能力生成合法参数。
3. 使用参数数组启动进程，不经 shell 拼接，正确处理空格、中文和特殊字符路径。
4. 通过 FFmpeg `-progress` 机器可读输出计算进度、速度和剩余时间。
5. 用持久化队列管理排队、运行、暂停、取消、重试和崩溃恢复。
6. 先写临时文件，成功校验后再原子改名，避免留下“看似完成”的损坏文件。
7. 再次用 ffprobe 检查输出容器、流、时长、分辨率、帧率和目标约束。
8. 保存完整版本、能力、参数和日志，使问题可以复现。

---

## 4. GitHub 开源项目调研

### 4.1 项目快照

| 项目 | 路线 | Star（快照） | 许可证 | 主要启示 |
|---|---|---:|---|---|
| [HandBrake](https://github.com/HandBrake/HandBrake) | C 核心，多平台 GUI；整合 FFmpeg、x264/x265、SVT-AV1 | 24,263 | GPL-2.0（README 明示） | 成熟预设、跨平台、队列与设备兼容目标；适合研究产品模型，不宜直接嵌入闭源产品 |
| [Shutter Encoder](https://github.com/paulpacifico/shutter-encoder) | Java GUI + 自带运行时 + FFmpeg/MediaInfo 等工具链 | 2,713 | GPL-3.0 | 专业功能覆盖广，证明“编排多个成熟 CLI”可形成完整产品；也展示功能膨胀风险 |
| [LosslessCut](https://github.com/mifi/lossless-cut) | Electron/TypeScript + FFmpeg | 43,431 | GPL-2.0 | 无损剪切、转封装、流管理、命令透明度和跨平台 UI 值得借鉴 |
| [FastFlix](https://github.com/cdgriffith/FastFlix) | Python GUI + FFmpeg + 可选第三方硬件编码器 | 1,560 | MIT（应用仓库） | HDR、AV1 与多硬件后端能力矩阵；应用许可不代表捆绑组件许可 |
| [VidCoder](https://github.com/RandomEngy/VidCoder) | Windows C# GUI，基于 HandBrake 编码引擎 | 863 | GPL-2.0 | Windows 原生交互、HandBrake 引擎复用、预设/队列路线 |
| [FFmpeg Batch AV Converter](https://github.com/eibols/ffmpeg_batch) | Windows C# 前端，直接编排 FFmpeg | 1,563 | LGPL-2.1（仓库元数据） | 多文件并发、文件夹监控、任务变量、暂停/恢复与命令向导 |

说明：Star 只能反映公开关注度，不能单独代表代码质量、维护能力或适合复用程度。调研时这些仓库均未归档，并在 2026 年有活动或发行版本。

### 4.2 深入观察

#### HandBrake

优势：

- 对“从几乎任意输入到少数现代输出”的产品边界控制得好；
- 预设按设备/用途组织，降低编码知识门槛；
- 自有扫描、任务模型和跨平台 GUI，工程成熟度高；
- 软件编码能力和质量控制强。

局限/启示：

- 不是任意 FFmpeg 功能的通用外壳；
- 用户若需要流复制、复杂封装和专业工具链，往往转向其他产品；
- GPL-2.0 对闭源复用有约束，应借鉴交互与模型，不直接复制代码。

#### Shutter Encoder

优势：

- 同时覆盖视频、音频、图片、字幕、响度、LUT、水印、无损操作与媒体分析；
- 批处理和预设适合专业后期流程；
- 通过 FFmpeg、MediaInfo、ExifTool、tsMuxeR、Whisper 等成熟组件组合扩展能力。

局限/启示：

- 功能越多，参数冲突、测试组合、安装体积和新手理解成本越高；
- MVP 不应同时引入下载、AI、DVD、RAW、背景移除等非核心能力；
- 多二进制组件会显著增加供应链、许可证和安全更新成本。

#### LosslessCut

优势：

- 直接流复制使裁剪、重排、抽取和换封装极快；
- 时间线、关键帧跳转、轨道选择和命令日志做得贴近实际媒体问题；
- Electron + FFmpeg 证明 Web 技术桌面壳不会成为转码性能瓶颈，重计算仍在独立进程。

局限/启示：

- 无损切割受关键帧约束，“Smart Cut”本质上需要局部重编码；
- 官方明确当前没有大规模批量导出，说明时间线编辑模型与批处理模型并不天然统一；
- 不应把“无损剪切器”与“批量转码器”在 MVP 中做成同等主流程。

#### FastFlix

优势：

- 对 x264/x265、SVT-AV1、AOM、rav1e 和不同硬件编码器做能力分层；
- 对 HDR10/HDR10+、10-bit、BT.2020 等专业属性有较强意识；
- 清楚展示不同后端不是功能完全等价。

局限/启示：

- 外部编码器需额外下载和配置，用户支持成本高；
- 应先用 FFmpeg 内建后端覆盖主流场景，再决定是否接入 NVEncC/QSVEncC 等专用工具。

#### VidCoder 与 FFmpeg Batch

优势：

- VidCoder 说明 Windows 原生 GUI + 成熟引擎可提供稳定体验；
- FFmpeg Batch 的多任务并发、参数变量、文件夹监控和后处理对专业用户价值高。

局限/启示：

- “允许任意参数”必须隔离在专家模式，不能破坏可验证的结构化任务模型；
- 自动关机、执行外部程序、URL 下载会扩大安全边界，不进入第一版。

### 4.3 可复用策略

建议分三层看待“复用”：

- **可直接依赖**：FFmpeg/ffprobe 可执行程序或动态库，但必须按选定构建满足 LGPL/GPL；具体编码器还涉及各自许可。
- **可研究设计**：预设结构、队列状态、流映射、错误分类、能力探测和交互模式。
- **慎重复制代码**：HandBrake、Shutter Encoder、LosslessCut、VidCoder 等 GPL 项目代码若并入并发布，可能要求派生作品按 GPL 开源。闭源计划下不应直接复制。

FastFlix 的 MIT 只覆盖该应用自身代码，不自动覆盖 FFmpeg、x265 或第三方硬件工具。任何复用都要生成软件物料清单（SBOM）并逐项核验。

---

## 5. 主流厂商转码产品研究

### 5.1 共同原理

主流云转码的编码链路仍是“解封装 -> 解码 -> 滤镜 -> 编码 -> 封装”。其优势主要来自规模化工程，而不是基础原理不同：

```text
对象存储输入
  -> API/控制台提交异步作业
  -> 模板与工作流展开多个输出
  -> 队列调度和弹性计算
  -> 分片并行/专用编码器/质量优化
  -> MP4 或 HLS/DASH/CMAF 打包
  -> 对象存储 + CDN
  -> 回调/消息/监控/质检
```

桌面端最值得借鉴的是“作业、预设、模板、队列、事件和验证”六个抽象，而不是云端 IAM、对象存储和分布式系统本身。

### 5.2 厂商对比

| 厂商/产品 | 主要架构与能力 | 公开优势 | 对桌面产品的启示 |
|---|---|---|---|
| AWS Elemental MediaConvert | 文件型异步作业；Input/Output、Preset、Job Template、Queue；支持广播编码、HDR、音频、字幕、DRM、ABR | QVBR 按画面复杂度分配比特；Professional tier 支持多遍高质量与加速转码；AWS 媒体生态完整 | 区分单输出预设与整项任务模板；质量目标应是一等参数；高级能力分层 |
| Google Cloud Transcoder API | Cloud Storage 输入输出；REST/RPC 异步作业；Job Template；HLS/DASH、HDR、overlay、EditList、加密、字幕、雪碧图、Pub/Sub | 配置结构清晰，IAM 与云生态结合；批处理与低延迟作业模式；文档明确普通异步模式不适合交互等待 | 任务配置应结构化而非拼字符串；UI 必须明确“等待型本地任务”的实时状态 |
| 阿里云媒体处理 MPS | OSS + 控制台/API/SDK + 弹性处理；工作流、模板、转码/打包/加密/AI | “窄带高清”按内容分析降低码率；长视频切片并行的倍速转码；国内生态、格式和 AI 能力广 | 可在后续加入内容感知预设和分段并行，但先做好本地单任务可靠性 |
| 腾讯云媒体处理 MPS | COS 输入；服务编排自动触发；CMQ 事件；自研编码内核 + AI；质检、增强、转码、截图、水印 | 高可编排；极速高清；长视频最高 30 倍分布式加速；AI 修复/字幕/横转竖/审核；质检评测 | “转码后验证”和条件化工作流是差异点；AI 功能应作为独立后续模块 |
| Cloudflare Stream | 上传后自动编码、存储并通过全球网络分发；API 管理 VOD/直播；H.264 ABR 360p～1080p | 从上传到播放的一体化、运维负担低、签名 URL、播放器和分析一体 | 它优化的是开发者集成和分发，不是专业参数自由度；本产品应专注本地控制和隐私 |
| Microsoft Azure Media Services | 曾提供编码、直播、分发与保护 | 已于 **2024-06-30** 退役，微软建议迁移 Bitmovin、MediaKind、Ravnur 等伙伴方案 | 第三方云集成不可成为本地产品的核心依赖；要设计可替换适配层 |

### 5.3 厂商优势的实质

1. **弹性并发**：大量作业排队时按需扩容，本地桌面受单机 CPU/GPU 限制。
2. **分片并行**：长视频按 GOP/片段拆分到多个节点编码，再拼接；需处理边界一致性、音频连续性和码率波动。
3. **内容感知编码**：分析运动、纹理、场景、人脸/ROI，动态选择码率或增强策略。
4. **自适应码率输出**：一次任务生成多分辨率、多码率 ladder，并打包 HLS/DASH/CMAF。
5. **媒体供应链**：对象存储触发、模板、回调、审计、DRM、CDN、监控和内容审核形成闭环。
6. **质量评测**：以 VMAF/SSIM/PSNR 或无参考模型做质量与异常检测，而不是只看命令退出码。

### 5.4 本地产品不应照搬的部分

- 本地文件无需先复制到“应用素材库”，可以保存路径和指纹；
- 单机并发不应越多越好，多个重型编码会争抢 CPU/GPU、内存和磁盘；
- 本地用户更看重暂停、取消、睡眠恢复、磁盘空间和源文件安全；
- DRM、CDN、审核、多人权限不是本地转码 MVP 的核心；
- 分片并行对短视频收益小，且可能影响一致性，建议 P2 再做长视频实验。

---

## 6. 产品定义

### 6.1 产品愿景

让用户不需要理解容器、编码器和复杂命令，也能在本机可靠地把视频转换为目标设备或平台可用的文件；同时让进阶用户知道工具实际做了什么，并能复现结果。

### 6.2 核心用户与场景

| 用户 | 主要任务 | 当前痛点 | 产品价值 |
|---|---|---|---|
| 普通用户 | 视频减小体积、转换 MP4、解决无法播放 | 不理解编码参数，容易选错格式 | 目标导向预设、一键兼容输出 |
| 内容运营 | 批量生成横版/竖版和不同清晰度 | 重复操作多、命名混乱 | 批量队列、模板、命名规则 |
| 摄影/创作者 | H.265/手机 VFR 转为剪辑友好格式 | 色彩、帧率、旋转、音轨容易出错 | 媒体分析、明确的中间格式预设、元数据保护 |
| 进阶用户 | 控制编码器、码率、GOP、流映射 | GUI 工具不透明或限制多 | 专家设置、实际命令与完整日志 |
| 隐私敏感用户 | 处理课程、会议、内部素材 | 不希望上传第三方云 | 默认离线、无需账号、无素材上传 |

### 6.3 产品原则

- 源文件安全优先于便利。
- 默认设置必须可解释且有合理结果。
- 只展示本机实际可用的能力。
- 高级参数不能绕过基础合法性校验，除非显式进入“原始参数实验区”。
- 失败必须给出下一步，而不是只展示 FFmpeg stderr。
- 本地优先，无账号也能完成全部核心功能。

---

## 7. 功能需求

优先级定义：P0 为 MVP 必须，P1 为首个稳定版建议，P2 为后续能力。

### 7.1 导入与分析

| ID | 优先级 | 需求 | 验收要点 |
|---|---:|---|---|
| FR-001 | P0 | 拖放/选择一个或多个文件 | 支持中文、空格、emoji、长路径；重复文件可识别 |
| FR-002 | P0 | 导入文件夹 | 可选择是否递归；过滤隐藏文件和不支持文件 |
| FR-003 | P0 | 自动分析媒体 | 展示容器、时长、尺寸、帧率、码率、编码、位深、色彩、音轨、字幕、旋转、文件大小 |
| FR-004 | P0 | 输入有效性检查 | 无法读取、无视频流、损坏头、权限不足时给出分类错误 |
| FR-005 | P1 | 输入预览 | 能播放常见格式或生成短预览；预览失败不阻止转码 |

### 7.2 输出设置

| ID | 优先级 | 需求 | 验收要点 |
|---|---:|---|---|
| FR-010 | P0 | 目标导向预设 | 至少提供“通用 MP4”“更小体积”“高质量归档”“网页播放”“剪辑中间格式” |
| FR-011 | P0 | 容器/视频/音频编码选择 | 只允许合法组合，非法组合即时解释 |
| FR-012 | P0 | 质量模式 | 支持 CRF/CQ 与目标码率；目标体积自动换算平均码率 |
| FR-013 | P0 | 尺寸处理 | 原尺寸、指定宽高、最长边、百分比；保持比例；偶数尺寸自动修正 |
| FR-014 | P0 | 帧率处理 | 保持源帧率、常见固定帧率；明确 VFR -> CFR 的影响 |
| FR-015 | P0 | 音频设置 | 选择/保留音轨、AAC/Opus、码率、采样率、声道；可仅导出音频 |
| FR-016 | P0 | 字幕设置 | 复制软字幕、移除字幕；不兼容时解释 |
| FR-017 | P0 | 转封装/流复制 | 合法时不重编码；界面明确显示“无质量损失”及限制 |
| FR-018 | P0 | 输出目录与命名 | 默认源目录子目录；支持后缀和冲突自动编号；禁止静默覆盖 |
| FR-019 | P1 | 裁剪/补边/旋转 | 提供常见比例及横竖屏适配，不拉伸画面 |
| FR-020 | P1 | 字幕烧录与水印 | 支持字体缺失检查和预览；进入滤镜链后必须重编码 |
| FR-021 | P1 | 响度归一化 | 支持 EBU R128/目标响度，必要时两阶段分析 |
| FR-022 | P1 | HDR 策略 | 保留 HDR、转 SDR、警告不兼容链路；禁止静默丢元数据 |
| FR-023 | P1 | 专家模式 | Profile、Level、GOP、B 帧、像素格式、两遍编码等 |

### 7.3 硬件能力

| ID | 优先级 | 需求 | 验收要点 |
|---|---:|---|---|
| FR-030 | P0 | 启动时能力探测 | 获取 FFmpeg 版本、编码器、解码器、滤镜、硬件设备 |
| FR-031 | P0 | 实际短测 | 对候选硬件编码器运行 1～2 秒合成源测试，不只解析 `-encoders` |
| FR-032 | P0 | 自动/手动选择 | 默认“自动”，同时显示实际将使用 CPU、NVIDIA、Intel 或 AMD |
| FR-033 | P0 | 失败处理 | 硬件初始化失败不静默换结果；提示原因并允许一键改用软件编码重试 |
| FR-034 | P1 | 能力详情页 | 显示支持的编码、位深、像素格式和测试结果，便于排障 |

### 7.4 队列与执行

| ID | 优先级 | 需求 | 验收要点 |
|---|---:|---|---|
| FR-040 | P0 | 批量队列 | 支持排序、移除、批量改预设、逐项覆盖参数 |
| FR-041 | P0 | 状态机 | Draft、Queued、Analyzing、Running、Paused、Succeeded、Failed、Canceled |
| FR-042 | P0 | 实时进度 | 显示进度、已用时、速度、预计剩余时间、当前阶段 |
| FR-043 | P0 | 取消/暂停/继续 | 取消应终止进程树；暂停/继续在当前 OS 可用时工作，并明确语义 |
| FR-044 | P0 | 并发控制 | 默认单重型任务；根据 CPU/GPU 后端分别限制并发；用户可调整 |
| FR-045 | P0 | 崩溃恢复 | 重启后保留队列；原 Running 任务标为 Interrupted，可重新执行 |
| FR-046 | P0 | 磁盘检查 | 开始前估算并检查空间；运行中空间不足给出明确错误 |
| FR-047 | P0 | 临时输出 | 输出到同卷临时文件，校验成功后原子改名；失败文件可选择保留用于排障 |
| FR-048 | P1 | 完成动作 | 系统通知、打开文件夹；不在 MVP 自动关机或执行任意外部程序 |

### 7.5 校验、日志与预设

| ID | 优先级 | 需求 | 验收要点 |
|---|---:|---|---|
| FR-050 | P0 | 输出校验 | ffprobe 能打开；存在预期流；时长偏差在阈值内；尺寸、编码符合任务 |
| FR-051 | P0 | 结构化错误 | 将常见 stderr 映射为磁盘、权限、解码、编码器、参数、驱动、文件损坏等类别 |
| FR-052 | P0 | 任务日志 | 保存应用版本、FFmpeg 版本、能力、参数数组、标准错误与校验结果 |
| FR-053 | P0 | 用户预设 | 创建、复制、重命名、导入导出；内置预设只读且可复制修改 |
| FR-054 | P0 | 预设版本 | 预设包含 schemaVersion；升级时迁移或提示不兼容字段 |
| FR-055 | P1 | 命令透明度 | 显示可复制的等价 FFmpeg 命令，同时对路径做展示层转义 |
| FR-056 | P1 | 对比摘要 | 完成后展示体积变化、码率、时长、分辨率和处理耗时 |

### 7.6 后续能力

- P1：区间裁剪、章节、简单拼接、图片序列、GIF/WebP、文件夹监控。
- P2：HLS/DASH 多码率 ladder、VMAF/SSIM 抽样质量报告、长视频分段并行。
- P2：Whisper 字幕、智能横转竖、超分和降噪，但必须按独立可下载模块管理模型体积与许可。
- P2：可选云端执行适配器，任务模型与本地后端共用，凭据存入系统密钥库。

---

## 8. 非功能需求

| ID | 类别 | 要求 |
|---|---|---|
| NFR-001 | 安全 | 永不静默覆盖源文件；参数以数组传入子进程，禁止经 shell 执行用户路径 |
| NFR-002 | 隐私 | 核心功能完全离线；无账号要求；默认不上传媒体、路径、文件名或日志 |
| NFR-003 | 响应性 | 基准 PC 冷启动目标 < 5 秒；扫描/转码期间 UI 不冻结；进度刷新间隔 <= 1 秒 |
| NFR-004 | 可恢复性 | 应用异常退出后队列与配置不损坏；任务写入采用事务；输出采用临时文件策略 |
| NFR-005 | 兼容性 | 支持 Unicode 与 Windows 长路径；首发覆盖 Windows 10 22H2、Windows 11 当前受支持版本 |
| NFR-006 | 可维护性 | UI、领域任务模型、命令编译器、进程执行器、FFmpeg 适配器相互隔离 |
| NFR-007 | 可诊断性 | 每个任务有唯一 ID；日志可打包导出并在用户确认后脱敏路径 |
| NFR-008 | 可访问性 | 键盘可完成主要流程；进度和状态不只依赖颜色；支持系统缩放和高对比度 |
| NFR-009 | 国际化 | 首发简体中文，文本与预设说明可国际化；技术术语提供简短解释 |
| NFR-010 | 发布安全 | 安装包和 FFmpeg 二进制校验哈希；Windows 代码签名；依赖生成 SBOM |
| NFR-011 | 更新 | 应用更新与 FFmpeg 安全更新可独立追踪；自动更新必须可关闭并可回滚 |
| NFR-012 | 规模 | 队列至少容纳 1,000 个任务；列表虚拟化；大型文件不整体读入内存 |

建议基准机：Windows 11、6 核 CPU、16 GB 内存、SSD；性能指标需区分“应用开销”和“编码器速度”，不能承诺统一倍速。

---

## 9. 推荐技术方案

### 9.1 方案比较

| 方案 | 优势 | 代价 | 适用判断 |
|---|---|---|---|
| Tauri 2 + React/TypeScript + Rust | 安装体积和内存较低；Rust 适合进程、文件和并发；跨平台 | 团队需掌握 Rust；sidecar、签名、跨平台进程控制需额外工程 | **推荐**，适合长期产品化 |
| Electron + React/TypeScript | 开发快、生态成熟；LosslessCut 已验证；播放器和跨平台经验多 | 安装体积、内存和更新包更大；需加强 Electron 安全边界 | JS 团队且交付速度优先时的务实备选 |
| .NET 8 + WPF/WinUI | Windows 集成、进程控制与原生体验好 | 跨平台基本需重写 UI；WinUI 发布链更复杂 | 确定长期只做 Windows 时可选 |
| Qt/C++ | 原生、跨平台、性能强，媒体生态成熟 | 开发成本和 UI 迭代成本高；内存安全要求更高 | 有成熟 C++/Qt 团队时可选 |

本报告推荐 **Tauri 2 + React/TypeScript + Rust + FFmpeg sidecar + SQLite**。如果当前团队只有 Web/Node 能力，则改用 Electron，不改变下面的领域架构。

### 9.2 逻辑架构

```text
┌─────────────────────────────────────────────┐
│ UI：导入 / 媒体信息 / 预设 / 队列 / 日志    │
└──────────────────────┬──────────────────────┘
                       │ typed command/event
┌──────────────────────▼──────────────────────┐
│ 应用服务：任务用例、状态机、设置、通知       │
├─────────────────────────────────────────────┤
│ 领域层：MediaInfo、Preset、Job、OutputSpec   │
│         Capability、Validation、Error        │
├─────────────────────────────────────────────┤
│ 基础设施                                    │
│ SQLite │ ffprobe │ Command Compiler │ Runner │
│ Files  │ Capability Probe │ Post Validator   │
└──────────┬─────────────────────────────┬─────┘
           │                             │
      FFmpeg sidecar                OS / GPU / FS
```

### 9.3 关键设计决定

#### 结构化配置，而非保存命令字符串

任务保存 `InputRef + OutputSpec + PresetSnapshot + CapabilitySnapshot`。执行前由命令编译器生成参数数组。这样才能：

- 在版本升级时迁移字段；
- 提前检查容器/编码组合；
- 在不同 OS 和硬件后端上生成不同实现；
- 可靠展示、重试和审计任务。

#### 队列状态持久化

SQLite 建议至少包含：

- `jobs`：输入、输出、状态、优先级、进度、时间、错误；
- `job_specs`：任务配置快照 JSON；
- `presets`：系统/用户预设与 schema 版本；
- `capability_snapshots`：FFmpeg 与硬件探测结果；
- `artifacts`：临时/最终文件和校验结果；
- `events`：关键状态变化，便于恢复与诊断。

#### 进程管理

- 不通过 `cmd.exe /c` 或 PowerShell 拼接命令；
- 捕获 stderr 与 `-progress pipe:1`，分别用于日志和结构化进度；
- Windows 使用 Job Object 或等价机制确保取消时结束整个进程树；
- pause/resume 需要明确是挂起进程，可能导致硬件编码器/网络输入超时；第一版可仅保证“取消 + 重新开始”，暂停作为受支持能力实验；
- 应用退出时先请求优雅结束，超时后再终止。

#### FFmpeg 分发

候选方式：

1. 捆绑已审计的固定版本：体验稳定、易复现，但安装包大且要及时发布安全更新。
2. 首次启动下载：安装包小，但首次体验依赖网络，需要哈希/签名校验和镜像策略。
3. 使用系统 FFmpeg：许可分离清楚，但版本和功能不可控，不适合普通用户默认路径。

建议 MVP 捆绑固定版本，并在“关于/许可证”展示构建配置、源码对应版本和许可文本。专家可选择自定义 FFmpeg，但应标记为“不受支持配置”。

### 9.4 预设设计

内置预设不应只叫“CRF 23”。建议包含用户目标、兼容性和结果说明：

| 预设 | 建议实现 | 说明 |
|---|---|---|
| 通用 MP4 | H.264 8-bit 4:2:0 + AAC + MP4 Fast Start | 终端兼容优先 |
| 更小体积 | H.265 或 AV1 + AAC/Opus，质量模式 | 新设备兼容，编码较慢 |
| 快速导出 | 可用硬件 H.264/H.265 + CQ/VBR | 速度优先，界面显示硬件后端 |
| 网页播放 | H.264/AAC MP4 + Fast Start | 面向直接文件播放；HLS/DASH 留待 P2 |
| 高质量归档 | 软件编码、较慢 preset、10-bit（适用时） | 不等同数学无损 |
| 仅换封装 | stream copy | 不改变码流，快速且无质量损失 |

剪辑中间格式可能涉及 ProRes/DNxHR 及许可/平台差异，进入 P1 前单独评估。

---

## 10. 数据与接口草案

### 10.1 任务配置示例

```json
{
  "schemaVersion": 1,
  "input": { "path": "D:/media/input.mov" },
  "output": {
    "directory": "D:/media/converted",
    "nameTemplate": "{name}_web",
    "container": "mp4",
    "overwrite": "never"
  },
  "video": {
    "mode": "transcode",
    "codec": "h264",
    "backend": "auto",
    "rateControl": { "type": "quality", "value": 21 },
    "scale": { "mode": "fit", "maxWidth": 1920, "maxHeight": 1080 },
    "frameRate": "source",
    "pixelFormat": "yuv420p"
  },
  "audio": {
    "tracks": "all",
    "codec": "aac",
    "bitrateKbps": 192
  },
  "subtitles": { "mode": "copy-compatible" },
  "metadata": { "mode": "preserve-safe" },
  "flags": { "fastStart": true }
}
```

这只是领域模型示意，不应把不同编码器的 CRF/CQ 数值假定为可直接横向比较。命令编译器需要按后端映射。

### 10.2 错误模型

统一错误至少包括：

- `INPUT_NOT_FOUND`、`INPUT_UNREADABLE`、`UNSUPPORTED_INPUT`、`CORRUPT_MEDIA`；
- `INVALID_OUTPUT_COMBINATION`、`OUTPUT_EXISTS`、`DISK_FULL`、`PERMISSION_DENIED`；
- `DECODER_FAILED`、`ENCODER_UNAVAILABLE`、`HW_INIT_FAILED`、`FILTER_FAILED`、`MUX_FAILED`；
- `PROCESS_CRASHED`、`USER_CANCELED`、`APP_INTERRUPTED`、`OUTPUT_VALIDATION_FAILED`。

每个错误都需包含用户说明、技术原因、可执行建议、原始日志位置，不能只依赖字符串全文匹配。

---

## 11. 测试与验收策略

### 11.1 测试素材矩阵

建立可合法再分发的小型样本库，至少覆盖：

- MP4/MOV/MKV/WebM/TS；
- H.264/H.265/AV1/VP9；8-bit/10-bit；SDR/HDR；
- CFR/VFR；23.976/25/29.97/30/50/59.94/60 fps；
- 横屏、竖屏、旋转元数据、奇数尺寸、非方形像素；
- 单/多音轨、5.1、不同采样率；SRT/ASS/PGS 等字幕；
- 时间戳异常、截断文件、零字节、只音频、无权限、超长路径和特殊字符文件名。

### 11.2 自动化层次

1. 命令编译器单元测试：给定结构化任务，断言参数数组和不变量。
2. 预设快照测试：升级不能无意改变内置预设结果。
3. 小样本集成测试：真实运行 ffprobe/FFmpeg，再校验输出。
4. 故障注入：磁盘不足、进程被杀、驱动不可用、目标文件存在、应用崩溃。
5. UI 端到端测试：导入 -> 选择预设 -> 入队 -> 完成 -> 打开结果。
6. 硬件实验室：至少覆盖无独显、Intel 核显、NVIDIA、AMD；硬件不可得的组合标记为未认证。

### 11.3 MVP 验收门槛

- 认证样本矩阵中 P0 转码成功率 >= 98%，其余失败均有正确分类且不损坏源文件；
- 取消任务后 3 秒内不再有相关 FFmpeg 子进程（正常本地文件场景）；
- 应用强制退出再启动，队列可恢复且无数据库损坏；
- 100 个混合任务连续执行，无 UI 冻结、句柄持续泄漏或临时文件失控；
- 输出校验能发现截断文件、缺失音轨、错误尺寸和明显时长偏差；
- 普通用户可在不打开专家模式的情况下完成通用 MP4 转换；
- 发布包包含所需许可证、源代码获取方式/构建信息和 SBOM。

---

## 12. 可行性与风险分析

### 12.1 技术风险

| 风险 | 概率/影响 | 缓解措施 |
|---|---|---|
| 不同 GPU/驱动表现不一致 | 高/高 | 实际短测；认证矩阵；明确回退；日志记录驱动和 FFmpeg 能力 |
| HDR/VFR/时间戳导致结果异常 | 中/高 | 默认保持源属性；高风险转换显式警告；专项样本和输出校验 |
| FFmpeg 参数组合爆炸 | 高/高 | 结构化领域模型；容器能力表；预设优先；专家参数隔离 |
| 大文件和长任务中断 | 中/高 | 持久化队列、临时文件、空间预检、崩溃恢复、可重试 |
| 进度/ETA 不准 | 高/中 | 读取 out_time 与实际时长；阶段化展示；前几秒不显示误导性 ETA |
| 应用更新破坏任务/预设 | 中/中 | schema 版本、迁移、快照测试和回滚 |

### 12.2 法务与分发风险

FFmpeg 官方说明：默认主要是 LGPL-2.1-or-later，但启用 GPL 组件或优化后，整个 FFmpeg 构建适用 GPL；`libx264` 是官方清单点名的 GPL 组件。闭源发布不能先随意下载一个“full build”再补许可证。

立项前必须决定：

- 产品是否开源、采用什么许可证；
- 采用独立 FFmpeg 可执行程序还是动态链接库；
- FFmpeg 的完整 configure 参数与第三方库清单；
- 是否需要 x264/x265、fdk-aac、VMAF 等组件，它们分别带来什么许可；
- H.264/H.265/AAC 等专利池在目标市场、分发规模和商业模式下的义务；
- Windows/macOS 商店对下载可执行代码、编解码器和 GPL 的政策。

可降低风险的工程做法：固定构建、保留源码对应版本、提供构建脚本与许可证、在应用关于页署名、生成 SBOM、对安全公告建立升级 SLA。以上不是法律意见，商业发布前需律师审核。

### 12.3 产品风险

- 同类免费工具多：只做“FFmpeg 套壳”缺乏持续价值。
- 新手与专家冲突：必须通过基础/专家两层设计解决，不能把所有参数堆在一个页面。
- “更小且无损”的错误预期：需要在设置旁展示取舍与估算，不用宣传语夸大。
- 功能膨胀：AI、下载、录屏、DVD、直播很容易稀释核心可靠性。

### 12.4 商业与运营可行性

本地方案没有持续转码算力成本，适合一次性付费、专业版授权或开源 + 支持服务。实际持续成本包括：

- Windows/macOS 代码签名与公证；
- 安装包、差分更新与下载 CDN；
- FFmpeg/编解码器安全更新；
- GPU 驱动兼容测试；
- 可选的崩溃上报和文档支持。

如以后接入云端转码，应让用户自带云账号或独立计量，否则高清长视频的上传、存储、出网和转码费用会改变商业模型。

---

## 13. 实施路线与估算

以下以 2 名全职开发 + 0.5 名产品/设计/测试投入估算；若只有 1 人，周期通常需要 1.7～2.2 倍。

### Phase 0：技术与许可 PoC（2 周）

- 最终确认开源/闭源路线和 FFmpeg 构建；
- ffprobe JSON 解析和统一媒体模型；
- 软件 H.264 + NVENC/QSV/AMF 能力短测；
- 结构化任务生成参数数组；
- 进度、取消、进程树结束；
- 10 个高风险样本验证 HDR/VFR/旋转/多音轨。

退出条件：许可证路径明确；三类硬件中至少能在现有测试机覆盖两类；核心任务可取消且不损坏源文件。

### Phase 1：Windows MVP（8～10 周）

- 主界面、导入、媒体分析、目标预设和输出设置；
- SQLite 队列、状态机、执行器、崩溃恢复；
- 软件/硬件编码、流复制；
- 日志、结构化错误、输出校验；
- 安装、签名、许可证页、自动化测试样本。

### Phase 2：稳定版（4～6 周）

- 预览、裁剪/补边/旋转、字幕烧录、响度归一化；
- HDR 策略、更多轨道控制、命令透明度；
- 完整硬件兼容实验、无障碍、性能与恢复测试；
- 可选自动更新和匿名崩溃上报（明确征得用户同意）。

### Phase 3：跨平台与高级能力（按需求立项）

- macOS 签名、公证、VideoToolbox；Linux 打包；
- HLS/DASH、多码率输出、质量评测；
- 长视频分段并行 PoC；
- AI 字幕/增强或可选云执行器。

### 13.1 人力粗估

| 工作包 | 人周 |
|---|---:|
| 产品/交互与预设定义 | 3～4 |
| 桌面 UI | 6～8 |
| 媒体模型/命令编译器 | 4～6 |
| 队列/进程/恢复 | 5～7 |
| 能力探测/硬件兼容 | 4～6 |
| 校验/日志/错误体系 | 3～4 |
| 测试、打包、签名、文档 | 5～7 |
| 合计（存在并行） | 30～42 人周 |

---

## 14. 立项决策与下一步

### 14.1 Go 条件

- 接受“FFmpeg 内核 + 自研产品层”的路线，不计划自研编码器；
- 接受 Windows 首发和严格 MVP 边界；
- 在编码前确定 FFmpeg 构建与产品许可证；
- 能获得至少 Intel/NVIDIA/AMD 中两类硬件测试环境；
- 产品差异化聚焦“易用、可靠、透明、本地隐私”，而非功能数量。

### 14.2 No-Go 条件

- 第一版就要求完整剪辑、直播、云协作、AI 修复和全平台；
- 闭源商业发布但不愿投入许可证/专利核验；
- 没有真实硬件与异常素材测试，只依赖开发机；
- 以“比 FFmpeg 更快”作为核心承诺，却没有专用编码器或分布式资源依据。

### 14.3 进入原型前需确认的产品决策

1. 产品是否开源、免费或闭源商业化。
2. 是否确定 Windows 为首发，最低系统版本是什么。
3. 第一目标用户是普通用户、内容运营还是专业后期。
4. 是否必须支持 ProRes/DNxHR/HDR10+ 等专业格式。
5. 是否捆绑 FFmpeg，还是允许首次下载。
6. 是否允许匿名崩溃/性能遥测；默认建议关闭并由用户主动开启。

建议下一份交付物是《MVP 产品规格说明 PRD + 低保真交互原型》，并同时做 Phase 0 PoC。两者并行，PoC 结果反向约束界面中真正可承诺的硬件与格式能力。

---

## 15. 参考资料

### 15.1 开源项目与底层技术

- FFmpeg About：https://ffmpeg.org/about.html
- FFmpeg CLI 文档（转码、流复制、filtergraph）：https://ffmpeg.org/ffmpeg.html
- ffprobe 文档：https://ffmpeg.org/ffprobe.html
- FFmpeg License and Legal Considerations：https://ffmpeg.org/legal.html
- NVIDIA Video Encode/Decode Support Matrix：https://developer.nvidia.com/video-encode-and-decode-gpu-support-matrix-new
- HandBrake 仓库与 README：https://github.com/HandBrake/HandBrake
- Shutter Encoder 仓库与 README：https://github.com/paulpacifico/shutter-encoder
- LosslessCut 仓库与 README：https://github.com/mifi/lossless-cut
- FastFlix 仓库与 README：https://github.com/cdgriffith/FastFlix
- VidCoder 仓库：https://github.com/RandomEngy/VidCoder
- FFmpeg Batch AV Converter 仓库与 README：https://github.com/eibols/ffmpeg_batch

### 15.2 厂商官方资料

- AWS MediaConvert 概览：https://docs.aws.amazon.com/mediaconvert/latest/ug/what-is.html
- AWS QVBR 指南：https://docs.aws.amazon.com/mediaconvert/latest/ug/qvbr-guidelines.html
- AWS Accelerated Transcoding：https://docs.aws.amazon.com/mediaconvert/latest/ug/accelerated-transcoding.html
- Google Cloud Transcoder API Overview：https://cloud.google.com/transcoder/docs/concepts/overview
- 阿里云媒体处理产品介绍：https://help.aliyun.com/zh/mps/product-overview/what-is-apsaravideo-media-processing
- 阿里云媒体处理功能：https://help.aliyun.com/zh/mps/product-overview/features
- 腾讯云媒体处理产品概述：https://cloud.tencent.com/document/product/862/36401
- 腾讯云媒体处理产品功能：https://cloud.tencent.com/document/product/862/36402
- 腾讯云媒体处理产品优势：https://cloud.tencent.com/document/product/862/36403
- Cloudflare Stream Overview：https://developers.cloudflare.com/stream/
- Azure Media Services retirement guide：https://learn.microsoft.com/en-us/azure/media-services/latest/azure-media-services-retirement

### 15.3 证据边界

- 厂商对“节省 20%～60% 码率”“最高 30 倍速”等数字是其官方产品口径，依赖内容、参数、付费层级和输入条件，不能直接转化为本地产品承诺。
- GitHub Star、版本和活跃时间会变化；本报告仅用于产品方向判断。
- 编解码质量必须用自有代表性素材和盲测/VMAF 等方法验证，不能仅凭厂商或项目描述。
- 许可证章节是工程风险识别，不构成法律意见。
